Deadlock Python program

The Deadlock program accepts a user file input:

Input
	- file path can be specified in source text (input_filepath) or it defaults to current work directory.

Output
	- Outputs log information to output.log file in current work directory. Can set path in logging configuration.

Input and Output path needs to be set in the program (using input_filepath and logging configuration in the source text respectively).
